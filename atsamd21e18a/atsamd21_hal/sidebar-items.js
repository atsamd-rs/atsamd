initSidebarItems({"macro":[["dbgprint",""],["define_pins","This macro is a helper for defining a `Pins` type in a board support crate.  This type is used to provide more meaningful aliases for the various GPIO pins for a given board."]],"mod":[["clock","Configuring the system clock sources. You will typically need to create an instance of `GenericClockController` before you can set up most of the peripherals on the atsamd21 device. The other types in this module are used to enforce at compile time that the peripherals have been correctly configured."],["delay","Delays"],["gpio","Working with GPIO pins. The pins are associated with the PORT hardware.  This module defines a `split` method on the `PORT` type that is used to safely reference the individual pin configuration. The IO pins can be switched into alternate function modes, which routes the pins to different peripherals depending on the mode for the pin.   The pin configuration is reflected through the use of type states to make the interface (ideally, or at least practically) impossible to misuse."],["prelude","Import the prelude to gain convenient access to helper traits"],["sercom","Working with the SERCOM peripherals."],["time","Time units"],["timer","Working with timer counter hardware"]]});